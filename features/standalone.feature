Feature: Standalone Socket.IO Rack server
  Allows a user to run a standalone thin-rack-eventmachine-based daemon,
  that allows clients to queue and dequeue messages, using memory store
  for persistence by default.
